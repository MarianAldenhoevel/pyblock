# pyBlock dependencies
# Install with:  pip install -r requirements.txt

# Core image processing
Pillow>=10.0.0
Shapely>=2.0.0
scikit-image>=0.19.0
mapbox_earcut>=1.0.0
vtracer>=0.1.0  # optional alternative vectorizer

# External program dependencies (not pip-installable - install via OS package manager):
#
#   potrace  >= 1.16   https://potrace.sourceforge.net/
#     Ubuntu/Debian : sudo apt install potrace
#     macOS (brew)  : brew install potrace
#     Windows       : download from the project website
